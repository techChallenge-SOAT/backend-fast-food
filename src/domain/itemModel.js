class Item {
    constructor(id, categoria, nome, descricao, preco_unitario) {
      this.id = id;
      this.categoria = categoria;
      this.nome = nome;
      this.descricao = descricao;
      this.preco_unitario = preco_unitario;
    }
  
    // Métodos relacionados ao modelo de item
  }
  
  module.exports = Item;
  