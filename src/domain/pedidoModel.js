class Pedido {
    constructor(id, cliente_cpf, data_pedido, status) {
      this.id = id;
      this.cliente_cpf = cliente_cpf;
      this.data_pedido = data_pedido;
      this.status = status;
    }
  
    // Métodos relacionados ao modelo de pedido
  }
  
  module.exports = Pedido;
  